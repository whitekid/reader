{
  "name": "reader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute reader --file=schema.sql",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "dompurify": "^3.3.0",
    "linkedom": "^0.16.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@types/dompurify": "^3.2.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.3.3",
    "vite": "~5.4.21",
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0",
      "vite": "~5.4.21"
    }
  }
}
